{
  "name": "scripts",
  "version": "1.0.0",
  "description": "Using the CustomJS plugin, this folder contains JavaScript files that handle custom code. Obsidian will not show the scripts in this folder because they are .js file extension types. **Please use a text editor**",
  "main": "tifa.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^4.8.4"
  }
}
